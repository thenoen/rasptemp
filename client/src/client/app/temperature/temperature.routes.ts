import { Route } from '@angular/router';
import { TemperatureComponent } from './index';

export const TemperatureRoutes: Route[] = [
  {
    path: 'temperature',
    component: TemperatureComponent
  }
];
